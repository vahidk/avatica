Write the full monologue from that outline. Follow the guide's format exactly.

Start with the `# Characters` section (just the performer), then `# Scenes` (just the setting), then `# Script` — in that order.

- One physical cue per beat in italic parentheses directly under the beat header.
- Spoken line in quotes directly under the cue. Multiple sentences fine; make them sound SPOKEN.
- No character name labels on spoken lines — every line is the performer.
- SHOT notes are optional. Use at most one per distinct camera setup.
- No preamble before the `# Characters` section.

Read every spoken line aloud in your head before you commit it. If it sounds written, rewrite it.
