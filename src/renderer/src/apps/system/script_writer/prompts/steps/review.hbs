Do a quick sanity check on the script. Flag only real problems:

- Are there `# Characters`, `# Scenes`, and `# Script` sections at the top, in that order?
- Does every recurring character have an entry in the Characters section? Every unique location in the Scenes section?
- Does any line tell us how a character feels instead of showing it?
- Is any action line or dialogue line confusing or overwritten?
- Does total duration match the target?
- Does any scene have more than one SHOT note, or a SHOT note longer than 2 sentences?

Lead with:

VERDICT: PASS or REVISE

If REVISE, output the full revised script starting from the `# Characters` section. Plain, direct, no extra commentary. Do NOT rewrite for style or cleverness — only fix the specific problems.
