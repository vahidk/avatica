Create a detailed character based on this concept: {{concept}}

Provide all fields. Species defaults to human — set accordingly for non-human characters. For non-humans, skip identity/appearance fields that don't apply and use extra/features instead.

Keep every field concise. Personality and backstory should be 1–2 short sentences each. Other fields should be a few words or a single phrase. Do not write paragraphs.

{{#unless hasVoiceInput}}
For voice, describe pitch (low/medium/high), tone (warm/smooth/raspy/breathy/sharp/gravelly), pace (slow/moderate/fast).
{{/unless}}
